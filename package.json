{
  "name": "coredevkit",
  "private": true,
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "devkit": "packages/plugin/dist/cli.js"
  },
  "dependencies": {
    "@coredevkit/plugin": "workspace:*"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "format": "pnpm -r format"
  }
}
