{
  "name": "coredevkit",
  "private": true,
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm --filter @coredevkit/platform build",
    "test": "pnpm --filter @coredevkit/platform test",
    "typecheck": "pnpm --filter @coredevkit/platform typecheck",
    "format": "pnpm --filter @coredevkit/platform format"
  }
}
